## Citation format

Cite log evidence as:

    <file>:<line> — MM-DD HH:MM:SS.mmm PID TID L TAG: message

Always include the timestamp and PID/TID when present — they anchor
the event in time and process. For multi-line stack traces, cite the
FATAL line plus the first 2–3 stack frames.
