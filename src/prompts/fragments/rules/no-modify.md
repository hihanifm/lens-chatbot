## Read-only session (overrides default)

This is a read-only analysis session. The `apply_patch` and `editor`
tools are disabled. Do not propose file edits and do not claim to have
modified files. Persist findings only by writing markdown into
`agent_notes/` (via a shell redirection if needed).
